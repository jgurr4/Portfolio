
 -> Add backend code for routing.
 -> Add backend code to handle form submission data and sends to database.
 -> Add backend code to make it dockerized.
 -> Add a better profile pic.
 -> Finish getting crawl project functional.
 -> Finish getting MySQL certified.
 -> fix bug: when using Portfolio section in the smallest view, it creates a scrollbar at the bottom for some reason.
(After this I am ready to post online and add to server.)

 -> Add an arrow or something that helps employers know that the menu button is actually a button.
 -> Add animations to picture changes and menu changes
 -> Consider adding RancherOS as a technology on the list.